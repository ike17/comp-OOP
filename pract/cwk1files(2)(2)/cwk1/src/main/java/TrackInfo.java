/**
 * Program to provide information on a GPS track stored in a file.
 *
 * @author YOUR NAME HERE
 */
public class TrackInfo {

  public static void main(String[] args) {
    
    /* 
    if (args.length != 1) {
      System.out.println("Usage: java TrackInfo <file_path>");
      System.exit(1);
    }

    String filePath = args[0];
    Track track = new Track();
    track.readFromFile(filePath);

    System.out.println("Number of points: " + track.size());
    System.out.printf("Total distance: %.2f meters\n", track.totalDistance());
    System.out.printf("Lowest point: %s\n", track.lowestPoint());
    System.out.printf("Highest point: %s\n", track.highestPoint());
    System.out.printf("Average speed: %.2f km/h\n", track.averageSpeed());
  */
  }
}
